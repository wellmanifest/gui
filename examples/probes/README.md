# Legacy visual-probe/v0

Superseded by `wellmanifest.gui/page/v1` in [`../pages/`](../pages/).

These JSON dumps are the raw Chrome histograms used to emit the page
examples. Do not add new v0 probes; use `scripts/probe-visual.py`.
