{
  "spec": "autogrammar.gui/v1",
  "name": "Wellmanifest Universal GUI Autogrammar Specification",
  "rules": [
    {
      "name": "TOOLBAR_STRUCTURE",
      "pattern": "<div class=\"item-section-toolbar\">...</div>",
      "required_child_tags": [".panel-section-title", ".section-view-toolbar", ".item-view-switch"]
    },
    {
      "name": "CREATE_PRIMARY_BUTTON_LEFT",
      "pattern": "<SectionViewToolbar modes={modes} current={view} onChange={setView} />",
      "constraint": "primary .section-add-button left of .item-view-switch; create ids excluded from switch modes"
    },
    {
      "name": "URL_SYNC",
      "pattern": "updateUrlTabAndView(tab, view)",
      "constraint": "MUST be executed on every SectionViewToolbar onChange callback"
    },
    {
      "name": "NO_UNICODE_EMOJI_CONTROLS",
      "pattern": "<button>...",
      "constraint": "Control icons MUST use <Icon name='...' /> or <svg> rather than emoji symbols"
    }
  ]
}
